import { IconName } from "./recipe.types";

export type IconImageRendering = "-moz-initial" | "inherit" | "initial" | "revert" | "unset" | "-moz-crisp-edges" | "-webkit-optimize-contrast" | "auto" | "crisp-edges" | "pixelated" | undefined;

export interface IItemProp {
    overrideName?: string;
    imageRendering?: IconImageRendering;
}

export const ItemProps: Map<IconName, IItemProp> = new Map();
